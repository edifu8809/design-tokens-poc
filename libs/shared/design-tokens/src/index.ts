// This library has no TS runtime API — it compiles the JSON files under tokens/
// into dist/css/tokens.css (see build.js). Consume the CSS output directly, e.g.
// libs/shared/design-tokens/dist/css/tokens.css, as done in apps/demo-app/project.json.
export {};
